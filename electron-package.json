{
  "name": "get-jobs-desktop",
  "version": "1.0.0",
  "description": "Get Jobs 桌面应用 - 自动化求职平台",
  "main": "electron/main.js",
  "author": "GetJobs",
  "license": "MIT",
  "scripts": {
    "electron:dev": "concurrently \"npm run dev:frontend\" \"wait-on http://localhost:3000 && npm run dev:electron\"",
    "dev:frontend": "cd front && npm run dev",
    "dev:electron": "cross-env NODE_ENV=development electron .",
    "electron:build": "npm run build:frontend && electron-builder",
    "build:frontend": "cd front && npm run build:prod",
    "electron:preview": "npm run build:frontend && electron .",
    "dist": "npm run build:frontend && electron-builder --publish=always",
    "dist:win": "npm run build:frontend && electron-builder --win --publish=always",
    "dist:mac": "npm run build:frontend && electron-builder --mac --publish=always",
    "dist:linux": "npm run build:frontend && electron-builder --linux --publish=always"
  },
  "build": {
    "appId": "com.getjobs.desktop",
    "productName": "Get Jobs",
    "directories": {
      "output": "release"
    },
    "files": [
      "electron/**/*",
      "front/out/**/*",
      "build/libs/**/*.jar",
      "!node_modules/**/*"
    ],
    "extraResources": [
      {
        "from": "build/libs",
        "to": "app",
        "filter": ["*.jar"]
      },
      {
        "from": "src/main/resources",
        "to": "resources",
        "filter": ["**/*", "!**/*.java"]
      }
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ],
      "icon": "electron/assets/icon.ico"
    },
    "mac": {
      "target": ["dmg"],
      "icon": "electron/assets/icon.icns",
      "category": "public.app-category.productivity"
    },
    "linux": {
      "target": ["AppImage", "deb"],
      "icon": "electron/assets/icon.png",
      "category": "Utility"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": true,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Get Jobs"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "loks666",
        "repo": "get_jobs",
        "releaseType": "release"
      }
    ]
  },
  "dependencies": {
    "electron-log": "^5.1.2",
    "electron-updater": "^6.1.8"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "electron": "^28.1.0",
    "electron-builder": "^24.9.1",
    "wait-on": "^7.2.0"
  }
}
